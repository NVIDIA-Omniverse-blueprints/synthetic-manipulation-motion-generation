version: '3.8'

services:
  isaac-lab:
    image: nvcr.io/nvidian/isaac-lab:latest-2.0.1_brev
    privileged: true
    network_mode: host
    ports:
      - '49100:49100/tcp'  # Livestream
      - '47998:47998/udp'  # Livestream
      - '8888:8888'        # JupyterLab
    environment:
      ACCEPT_EULA: "Y"
    runtime: nvidia
    pull_policy: always
    ulimits:
      memlock: -1
      stack: 67108864
    shm_size: 1g
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: all
              capabilities: [gpu]
    volumes:
      - /home/ubuntu/docker/isaac-lab/cache/kit:/isaac-sim/kit/cache:rw
      - ./launch.sh:/workspace/isaaclab/launch.sh
      - ./notebook/generate_dataset.ipynb:/workspace/isaaclab/notebook/generate_dataset.ipynb
      - ./samples/annotated_dataset.hdf5:/workspace/isaaclab/datasets/annotated_dataset.hdf5
    user: root
    working_dir: /workspace/isaaclab
    entrypoint: ["/workspace/isaaclab/launch.sh"]
    restart: unless-stopped